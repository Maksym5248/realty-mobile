// import _ from 'lodash';

export function serialize(snapshot) {
  const newSnapshot = {
    // isIntroduced: snapshot.isIntroduced,
    // viewer: _.pick(snapshot, ['currency']),
  };

  return newSnapshot;
}
