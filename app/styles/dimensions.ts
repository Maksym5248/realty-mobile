/*
  const dimensions = {
    xxl: 24,
    xl: 20,
    lx: -,
    l: 16,
    ls: -,
    ml: -,
    m: 14,
    ms: -,
    sm: -,
    s: 12,
    xs: 10,
    xxs: -,
  };
*/

const indent = 4;

export const dimensions = {
  xxl: indent * 14,
  xl: indent * 12,
  lx: indent * 10,
  l: indent * 6,
  m: indent * 3,
  s: indent * 2,
  xs: indent,
};

export const elements = {
  tabBarHeight: indent * 14,
};
